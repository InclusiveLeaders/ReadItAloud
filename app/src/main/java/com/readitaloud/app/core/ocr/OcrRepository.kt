package com.readitaloud.app.core.ocr

import com.readitaloud.app.model.ScanResult

// TODO Story 2.2: Implement ML Kit Text Recognition v2 (bundled, offline)
// CRITICAL: Always return ScanResult — never throw exceptions
class OcrRepository {
    suspend fun recogniseText(/* bitmap: Bitmap */): ScanResult {
        TODO("Implement in Story 2.2")
    }
}
