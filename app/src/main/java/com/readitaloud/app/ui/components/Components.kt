package com.readitaloud.app.ui.components

// Shared reusable Compose components package — Story 3.1 implementation complete.
// Components implemented:
//   - ReadButton         (ReadButton.kt)         — 130dp amber button with pulse animation
//   - CameraViewfinder   (CameraViewfinder.kt)   — full-screen preview with amber corner brackets
//   - StateIndicator     (StateIndicator.kt)      — uppercase state label above READ button
//   - PlaybackTextDisplay (PlaybackTextDisplay.kt)— recognised text with word highlighting
//   - ChoiceButtonPair   (ChoiceButtonPair.kt)    — "Hear again" / "Scan new" post-read buttons
